    # templateHelpers: () ->
      # maps: @saved_maps.maps()
      
      
    # events:
      # 'click a.map-link': (e) ->
      #   url = $(e.currentTarget).attr('href')
      #   App.navigate url, true
